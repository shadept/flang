// Every collection under `std.collections` through one import: the growable list and the hash
// map, set, stack and deque built on them, each in its managed and unmanaged flavour (spec §9.4),
// the bit set, the iterator combinators, and the composites `Journal` and `MultiMap`.

pub import std.collections.bitset
pub import std.collections.deque
pub import std.collections.dict
pub import std.collections.iter
pub import std.collections.journal
pub import std.collections.list
pub import std.collections.multimap
pub import std.collections.set
pub import std.collections.stack
