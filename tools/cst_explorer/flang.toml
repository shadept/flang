[project]
name = "cst_explorer"
version = "0.1.0"
kind = "exe"
source = "src/**/*.f"
output = "build"

[dependencies]
flang_parser = { path = "../../lib/flang_parser" }
flang_core = { path = "../../lib/flang_core" }
