[project]
name = "dump_tokens"
version = "0.1.0"
kind = "exe"
source = "src/**/*.f"
output = "build"

[dependencies]
flang_core = { path = "../../lib/flang_core" }
flang_parser = { path = "../../lib/flang_parser" }
